# Presidio backend for SmartEmbedAgent.
#
# Installed automatically by the OpenClaw skill the first time a
# recognizer="presidio" config is used. Can also be installed manually:
#
#     pip install -r requirements-presidio.txt
#     python -m spacy download en_core_web_lg
#
# Note: this is a heavy install (~600MB total: presidio core + spaCy +
# the en_core_web_lg model). Skip it if you don't need Presidio's
# extended entity coverage — the default "legacy" recognizer is already
# layered with regex + dslim NER + the India region pack.

presidio-analyzer>=2.2.0,<3.0.0
presidio-anonymizer>=2.2.0,<3.0.0
spacy>=3.5.0,<4.0.0
